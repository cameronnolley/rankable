import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#CFCFCF",
            contrastText: "#fff"
        },
        secondary: {
            main: "#ff9800",
            contrastText: "#fff"
        }
    }
});

export default theme;